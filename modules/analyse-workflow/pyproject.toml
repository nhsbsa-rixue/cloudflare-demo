[project]
name = "analyse-workflow"
version = "0.1.0"
description = "Cloudflare Python Workflow for analysis tasks"
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
dev = [
    "workers-py",
    "workers-runtime-sdk",
]
