{
  "name": "@cloudflare-demo/infrastructure",
  "version": "1.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "bootstrap:api-key": "node --experimental-strip-types ./src/scripts/bootstrap-api-key.ts",
    "bootstrap:database": "node --experimental-strip-types ./src/scripts/create-d1-database.ts",
    "bootstrap:r2": "node --experimental-strip-types ./src/scripts/create-r2-bucket.ts",
    "bootstrap:access": "node --experimental-strip-types ./src/scripts/setup-zero-trust-access.ts",
    "bootstrap:workflow": "node --experimental-strip-types ./src/scripts/create-workflow.ts",
    "check": "tsc --noEmit",
    "lint": "biome check .",
    "format": "biome format --write ."
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "cloudflare": "^4.0.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "wrangler": "4.120.0"
  }
}
